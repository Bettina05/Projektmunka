@using Lingarix_Database.Entities
@model List<UserRangList>

@{
    ViewData["Title"] = "Ranglista";
}

<h3></h3>
<table>
    <tr>
        <th>Helyezés</th>
        <th>Felhasználónév</th>
        <th>Pontszám</th>
    </tr>
    @for (int i = 0; i < Model.Count; i++)
    {
        <tr>
            <td>@(i+1)</td>
            <td>@Model[i].Username</td>
            <td>@Model[i].Points</td>
        </tr>
    }
</table>
